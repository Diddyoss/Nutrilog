# Frontend (safe to expose — protected by Supabase RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — used by /api/analyze-food on Vercel. NEVER prefix with VITE_.
OPENROUTER_API_KEY=
# Optional — any vision-capable OpenRouter model. Defaults to google/gemini-3-flash-preview.
OPENROUTER_MODEL=
