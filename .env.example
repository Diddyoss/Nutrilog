# Frontend (safe to expose — protected by Supabase RLS)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side only — used by /api/analyze-food on Vercel. NEVER prefix with VITE_.
ANTHROPIC_API_KEY=
